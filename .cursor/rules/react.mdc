---
description: React 技术领域规则，按中级工程师视角撰写，注重实用性与快速上手
globs: ["react/**/*.md"]
alwaysApply: false
---
# React 技术领域规则（中级工程师视角）

## 核心定位

以中级工程师的视角学习和使用 React，注重实用性和快速掌握：

| 要求 | 说明 |
|-----|------|
| **视角** | 中级工程师视角，能独立完成业务需求 |
| **场景** | 示例结合真实 Web 业务场景（组件化、状态管理、基础性能优化等） |
| **代码** | 使用 **现代 React**（Hooks、函数组件），避免过时的 Class 组件 |
| **深度** | 达到中级水平，重点掌握**什么时候用**和**如何使用** |
| **关联** | 必要时与其他方案对比，帮助选型 |

### 学习目标

```
学 React 组件 → 会用组件、JSX、Props，能拆分和复用组件
学 React Hooks → 会用 useState、useEffect、自定义 Hooks
学状态管理 → 知道什么时候用 Context、什么时候用状态库
学 React 路由 → 会用 React Router 实现页面跳转和参数传递
学性能优化 → 知道常见性能问题和基础优化方法
```

## 写作三原则

| 原则 | 要求 |
|-----|------|
| **通俗易懂** | 用生活类比和简单例子讲清概念，避免堆砌术语 |
| **实用为主** | 重点讲**什么时候用**和**如何使用**，适当说明原因 |
| **场景明确** | 结合真实业务场景，说明适用情况和常见坑 |

### 写作风格示例

```
❌ 不好：useState 是 React 的状态管理 Hook，返回状态值和更新函数。

✅ 好的：

**什么时候用 useState？**
- 需要在组件里记住会变化的数据（如输入框内容、开关状态、列表数据）
- 数据变化时需要自动更新 UI

**如何使用？**
const [count, setCount] = useState(0)  // count 是状态，setCount 用来更新

**注意事项：**
- 状态只在当前组件里有效
- 要跨组件共享数据，用 Context 或把状态提升到父组件
```

## 中级工程师内容四维度

每个技术点应尽量覆盖以下维度：

| 维度 | 说明 | 示例 |
|-----|------|------|
| **是什么** | 用大白话解释概念 | useState 是用来在组件里记住数据的 |
| **什么时候用** | 明确适用场景 | 需要记住会变化的数据，且数据变化时要更新 UI |
| **如何使用** | 提供完整代码示例 | 包含导入、定义、使用的完整代码 |
| **常见问题** | 新手容易犯的错误和解决方法 | 不要直接修改状态、依赖数组不要漏 |

## 文档结构

### 两层文档体系

```
react/[技术名称]/
├── README.md      # 导航：快速了解这个技术是什么、什么时候用
├── 1-基础篇.md    # 基础：是什么、什么时候用、如何使用、基本示例
└── 2-进阶篇.md    # 进阶：高级用法、性能优化、最佳实践、常见问题
```

### 技术分类与学习价值

| React 技术 | 学了能做什么 |
|-----------|-------------|
| **组件基础** | 会拆分组件、使用 Props 传数据、渲染列表和条件 UI |
| **Hooks** | 会用 useState 管理状态、useEffect 处理副作用、自定义 Hooks 复用逻辑 |
| **Context API** | 会跨层级传递数据，避免 Props 层层传递 |
| **Router** | 会用 React Router 实现页面跳转、参数传递、嵌套路由 |
| **状态管理** | 知道什么时候用本地状态、什么时候用 Redux/Zustand |
| **性能优化** | 知道用 memo、useMemo、useCallback 避免不必要的渲染 |
| **样式方案** | 会用 CSS Modules 或 Tailwind 写样式 |
| **请求与数据** | 会用 fetch/axios 请求数据、用 React Query 管理请求状态 |

### 各篇内容要求

**基础篇**（让人会用）
- **是什么**：用大白话解释概念，必要时用生活类比
- **什么时候用**：列出具体使用场景
- **如何使用**：提供完整的 TypeScript + Hooks 代码示例
- **常见问题**：新手容易犯的错误和解决方法

**进阶篇**（让人用好）
- **高级用法**：更复杂的场景和技巧
- **性能优化**：常见性能问题和解决方案
- **方案对比**：什么时候选 A、什么时候选 B
- **最佳实践**：项目中推荐的做法和需要避免的坑

### 实用检验（每篇末尾）

用几个实际问题检验是否掌握：
- 基础篇：3～5 题，检验基本使用
- 进阶篇：3～5 题，检验解决问题的能力

答案建议：**先说怎么做 → 简单说明为什么**

## 代码规范

### 语言选择

所有示例统一使用 **TypeScript + Hooks**，体现现代 React 写法。

### 现代 React 代码风格

```typescript
// ✅ 好的示例：TypeScript + Hooks 处理异步数据
interface User {
  id: string
  name: string
  email: string
}

function UserProfile({ userId }: { userId: string }) {
  const [user, setUser] = useState<User | null>(null)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<Error | null>(null)

  useEffect(() => {
    fetchUser(userId)
      .then(setUser)
      .catch(setError)
      .finally(() => setLoading(false))

    return () => {
      // 清理：取消请求等
    }
  }, [userId])

  if (loading) return <LoadingSpinner />
  if (error) return <ErrorView error={error} />
  if (!user) return <EmptyView />

  return (
    <div>
      <h1>{user.name}</h1>
      <p>{user.email}</p>
    </div>
  )
}
```

### 示例场景要求

**使用真实 Web 业务场景**：

```
❌ 不好：只有孤立的 const [count, setCount] = useState(0)，没有业务上下文。

✅ 好的：用户列表分页加载
function UserList() {
  const [users, setUsers] = useState<User[]>([])
  const [page, setPage] = useState(1)
  const [hasMore, setHasMore] = useState(true)

  const loadMore = async () => {
    const newUsers = await fetchUsers(page)
    setUsers(prev => [...prev, ...newUsers])
    setPage(p => p + 1)
    setHasMore(newUsers.length > 0)
  }

  useEffect(() => {
    const handleScroll = () => {
      if (/* 滚动到底部 */ && hasMore) loadMore()
    }
    window.addEventListener('scroll', handleScroll)
    return () => window.removeEventListener('scroll', handleScroll)
  }, [hasMore, page])

  return (
    <div>
      {users.map(user => <UserCard key={user.id} user={user} />)}
      {hasMore && <LoadingSpinner />}
    </div>
  )
}
```

### 示例自洽性

代码示例应**自包含**，读者能直接看懂：

```typescript
// ❌ 不好：onClick={handleClick} 未定义，读者不知道从哪来、做什么。

// ✅ 好的：事件处理在组件内定义，意图清晰
function TodoItem({ todo, onToggle }: { todo: Todo; onToggle: (id: string) => void }) {
  const handleToggle = () => onToggle(todo.id)

  return (
    <div onClick={handleToggle}>
      <input type="checkbox" checked={todo.completed} readOnly />
      <span>{todo.text}</span>
    </div>
  )
}
```

### 注释规范

注释说明**意图、取舍和易错点**：

```typescript
// ❌ 不好：setUsers(newUsers)  // 设置用户列表

// ✅ 好：说明「为什么」这样写
// React 依赖引用比较判断是否重渲染，需保持不可变：用新数组而非修改原数组
setUsers(prev => [...prev, ...newUsers])
```

**建议加注释的场景**：
- 不可变性、依赖数组等容易踩坑的地方
- 为什么选这个 API 而不是另一个
- 可能影响性能或可维护性的写法

## 图表规范

必要时使用 Mermaid 图表帮助理解：
- **流程图**：组件渲染流程、数据流向
- **结构图**：组件层次关系

### 示例：组件渲染流程

```mermaid
graph TB
    A[组件挂载] --> B[首次渲染]
    B --> C[useEffect 执行]
    C --> D[状态更新]
    D --> E[重新渲染]
    E --> F[组件卸载]
    F --> G[清理函数执行]
```

## 特殊内容要求

### 状态管理

- 说清 **什么时候用 useState**、**什么时候用 useReducer**、**什么时候用 Context**、**什么时候用 Redux/Zustand**
- 给出**正确示例**和**常见错误**

```typescript
// ❌ 错误：直接修改状态，React 检测不到变化
const [users, setUsers] = useState<User[]>([])
users.push(newUser)  // 不会触发重新渲染

// ✅ 正确：创建新数组
setUsers(prev => [...prev, newUser])  // 会触发重新渲染
```

### 性能优化

- 说明 **什么时候用 memo**、**什么时候用 useMemo**、**什么时候用 useCallback**
- 提醒避免过早优化，先保证功能正确

### Hooks

- 讲清 **什么时候需要自定义 Hooks**
- 说明 Hooks 规则（只在顶层调用、只在 React 函数中调用）

### 路由

- 说明 **如何用 React Router 实现页面跳转**、**如何传递参数**
- 提供嵌套路由的示例

## 禁止事项

- ❌ 讲太深的原理和源码细节
- ❌ 使用过时的 Class 组件（应以 Hooks 为主）
- ❌ 使用过时 API（如 `componentWillMount`）
- ❌ 示例中出现未定义的类型或变量
- ❌ 忽略常见错误（如依赖数组错误导致无限循环）
- ❌ 只给 JavaScript 示例，必须提供 TypeScript 版本

## 质量自检

写完后建议检查：
- [ ] 四维度是否覆盖？（是什么、什么时候用、如何使用、常见问题）
- [ ] 说明了明确的使用场景？
- [ ] 代码示例为 TypeScript + Hooks？
- [ ] 示例场景真实、完整、易懂？
- [ ] 说清了什么时候选这个方案？
- [ ] 提醒了常见错误和解决方法？

## React 核心思维（中级工程师）

### 关键概念

| 概念 | 要点 |
|-----|------|
| **声明式 UI** | 描述「数据变化时 UI 长什么样」，而非手动操作 DOM |
| **不可变数据** | 用新对象/数组替代修改原数据，React 才能检测到变化 |
| **单向数据流** | 数据从父组件传给子组件，事件从子组件传给父组件 |
| **组件化** | 把 UI 拆分成独立的组件，通过组合完成复杂功能 |
| **Hooks** | 在函数组件内使用状态和副作用，逻辑可以复用 |
