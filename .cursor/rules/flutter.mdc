---
description: Flutter 技术领域规则，双文档精简版（基础使用 + 原理讲解），强化 Flutter 核心哲学与实战深度
globs: ["Flutter/**/*.md"]
alwaysApply: false
---


# Flutter 技术领域规则

## 核心定位
**以 Flutter 专家视角，将技术点拆解为“生产力”与“内功”双维度。基础篇解决“如何最优实现”，原理篇解决“底层逻辑闭环”。内容需兼具工业级实战经验与引擎级原理洞察。根据知识点名称在Flutter文件夹下创建子文件夹 并将文档写入**

---

## 写作三原则

| 原则 | 要求 |
|-----|------|
| **生活类比** | 复杂概念必须有生活映射（如：BuildContext 是家庭住址，Widget 是建筑蓝图）。 |
| **实战导向** | 拒绝 Demo 代码。必须包含性能优化（RepaintBoundary）、包体积控制及生产环境踩坑录。 |
| **底层通透** | 触达三棵树（Widget/Element/RenderObject）协同、渲染管线、Dart 事件循环机制。 |

---

## Flutter 深度特供规则

1.  **布局灵魂法则**：涉及 UI 布局时，必须点透 **"Constraints down, Sizes up, Parent sets position"**（约束向下传递，尺寸向上传递，父级决定位置）。
2.  **状态流转图**：基础篇涉及交互时，必须使用 Mermaid 展示：`用户操作 -> 状态变更 (State) -> Widget 重建 (Rebuild)`。
3.  **多端差异警示**：作为跨平台专家，必须提示该技术在 iOS (Cupertino)、Web 或 Impeller 引擎下的表现差异。
4.  **生态选型批判**：引入第三方库时，需评估维护频率、平台兼容性及如果不用的替代成本。

---

## 文档结构：双文档体系

### 1. 基础使用篇（目标：拿到即用，用即最优）
- **技术演进**：该技术解决了什么历史痛点？（如：从命令式 UI 切换到声明式 UI 的阵痛）。
- **核心概念**：用大白话解释，并标注在 Widget 树中的地位。
- **最佳实践示例**：
    - **Dart 3.x 优先**（使用 Records, Pattern matching, Sealed classes）。
    - **代码自洽**：包含必要的类定义、Mixins 或 Import 说明。
    - **意图注释**：解释“为什么要用这个 API”而非语法本身。
- **性能/避坑策略**：
    - 给出该场景下减少 Rebuild 的具体方案。
    - 针对常见错误（如：垂直列表嵌套无边界、BuildContext 跨异步失效）提供 Checklist。
- **面试通关（实战类）**：3-5 题，侧重方案选型与问题解决思路。

### 2. 原理讲解篇（目标：看透本质，逻辑闭环）
- **设计思想**：探讨背后的设计模式（如：组合优于继承、不可变性哲学）。
- **核心流程图**：**必须使用 Mermaid** 绘制。包含渲染管线流程或对象生命周期。
- **源码洞察**：
    - 提取核心代码片段（<40 行）。
    - 重点解析 `mount`、`performLayout` 或 `paint` 等关键钩子。
- **边界认知**：明确说明技术的局限性、不适用场景及替代方案。
- **面试通关（原理类）**：3-5 题，侧重底层逻辑（答案格式：**结论 -> 原理 -> 举例**）。

---

## 代码与图表规范

- **代码质量**：强制支持 Null-safety，严格遵守 `flutter_lints`。
- **渲染视图**：
- **图表先行**：原理篇必须先有流程图再讲代码逻辑。
- **Kotlin 协同**：涉及 MethodChannel 时，必须同时提供 Dart 代码与对应的 Kotlin (Android) 实现。

## 禁止事项
- ❌ 禁止使用已废弃（Deprecated）的组件（如：FlatButton）。
- ❌ 严禁出现“详见官网”等甩锅行为，内容必须自闭环。
- ❌ 源码分析严禁大段粘贴，必须提炼核心逻辑并加注。
- ❌ 代码示例严禁出现未定义的“孤儿变量”。

---

## 质量自检
- [ ] 布局逻辑是否体现了“约束向下/尺寸向上”？
- [ ] 基础篇是否有 Mermaid 状态流转图？
- [ ] 原理篇是否涉及了“三棵树”中至少两棵的交互？
- [ ] 代码是否包含 Dart 3.x 特性且能通过编译？